# TaskMate — Firebase Infrastructure

This directory contains Firebase configuration, security rules, and database indexing rules for **TaskMate**.

## Roadmap

- **Phase 2:** Firebase Authentication integration and token verification foundation.
- **Phase 3:** Cloud Firestore rules enforcing user-scoped document access (`users/{userId}/tasks/{taskId}`).
- **Phase 3:** Composite index configuration (`firestore.indexes.json`).
